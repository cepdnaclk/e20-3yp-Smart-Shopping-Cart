package com.example.smartshoppingcart.Model;

/**
 * Enum representing the type of token being used in the application.
 * Currently, only the BEARER token type is defined, typically used for JWT (JSON Web Tokens).
 */
public enum TokenType {
    // BEARER token, the standard type for authorization tokens in RESTful APIs.
    BEARER,

}
